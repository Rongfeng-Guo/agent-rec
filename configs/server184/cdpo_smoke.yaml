model_name_or_path: ${LLAMAFACTORY_MODEL_NAME_OR_PATH}
stage: dpo
do_train: true
finetuning_type: lora
template: llama3
dataset_dir: ${BRIDGE_OUTPUT_DIR}/latest/cdpo_dataset
dataset: train
cutoff_len: 4096
per_device_train_batch_size: 1
gradient_accumulation_steps: 8
learning_rate: 5.0e-5
max_steps: 20
pref_loss: sigmoid
output_dir: outputs/server184_llamafactory/cdpo_smoke
